# FlashKit Basic Next.js Integration Example

This example demonstrates how an external application integrates `@flashkit/core`, `@flashkit/react`, and `@flashkit/ui`.

## Quickstart

```bash
# From repository root
pnpm install
pnpm --filter example-nextjs-basic dev
```

Visit `http://localhost:3001` in your browser.
